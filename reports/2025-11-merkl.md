Title: Code4rena | Keeping high severity bugs out of productionMerkl

Description: Code4rena is a competitive audit platform that finds more high-severity vulnerabilities, more quickly than any other auditing method.

Source: https://code4rena.com/reports/2025-11-merkl

---

[Skip Navigation](https://code4rena.com/reports/2025-11-merkl#skip-link)
- For ProjectsReady to secure your project?The best time to start is now.[Get started](https://go.code4rena.com/start?source=header-nav)Security solutions[Competitive auditWe invented the format that disrupted the web3 industry. Find out why top protocols trust C4.](https://code4rena.com/competitive-audit)[ZenithZenith assembles auditors with proven track records to secure your project.](https://code4rena.com/zenith)Resources[Docs](https://docs.code4rena.com)[Reports](https://code4rena.com/reports)
- [Competitive auditWe invented the format that disrupted the web3 industry. Find out why top protocols trust C4.](https://code4rena.com/competitive-audit)
- [ZenithZenith assembles auditors with proven track records to secure your project.](https://code4rena.com/zenith)
- [Docs](https://docs.code4rena.com)
- [Reports](https://code4rena.com/reports)
- For WardensFind bugs. Get paid.Put your security research skills to work.[Become a Warden](https://code4rena.com/register/account)Audits[Ongoing audits](https://code4rena.com/audits#active-audits)[Upcoming audits](https://code4rena.com/audits#upcoming-audits)[Past audits](https://code4rena.com/audits#completed-audits)[Bounties](https://code4rena.com/bounties)Leaderboard[Past 90 days](https://code4rena.com/leaderboard?timeframe=Last 90 days)[Past 365 days](https://code4rena.com/leaderboard?timeframe=Last 365 days)[All time](https://code4rena.com/leaderboard?timeframe=All time)Resources[Reports](https://code4rena.com/reports)[Help desk](https://code4rena.com/help)[Docs](https://docs.code4rena.com)[GitHub](https://github.com/code-423n4/)
- [Ongoing audits](https://code4rena.com/audits#active-audits)
- [Upcoming audits](https://code4rena.com/audits#upcoming-audits)
- [Past audits](https://code4rena.com/audits#completed-audits)
- [Bounties](https://code4rena.com/bounties)
- [Past 90 days](https://code4rena.com/leaderboard?timeframe=Last 90 days)
- [Past 365 days](https://code4rena.com/leaderboard?timeframe=Last 365 days)
- [All time](https://code4rena.com/leaderboard?timeframe=All time)
- [Reports](https://code4rena.com/reports)
- [Help desk](https://code4rena.com/help)
- [Docs](https://docs.code4rena.com)
- [GitHub](https://github.com/code-423n4/)
- [Support](https://code4rena.com/help)
- [Log in](https://code4rena.com/login)

## Ready to secure your project?
The best time to start is now.
[Get started](https://go.code4rena.com/start?source=header-nav)
- [Competitive auditWe invented the format that disrupted the web3 industry. Find out why top protocols trust C4.](https://code4rena.com/competitive-audit)
- [ZenithZenith assembles auditors with proven track records to secure your project.](https://code4rena.com/zenith)
[Competitive auditWe invented the format that disrupted the web3 industry. Find out why top protocols trust C4.](https://code4rena.com/competitive-audit)
We invented the format that disrupted the web3 industry. Find out why top protocols trust C4.
[ZenithZenith assembles auditors with proven track records to secure your project.](https://code4rena.com/zenith)
Zenith assembles auditors with proven track records to secure your project.
- [Docs](https://docs.code4rena.com)
- [Reports](https://code4rena.com/reports)
[Docs](https://docs.code4rena.com)
[Reports](https://code4rena.com/reports)

## Find bugs. Get paid.
Put your security research skills to work.
[Become a Warden](https://code4rena.com/register/account)
- [Ongoing audits](https://code4rena.com/audits#active-audits)
- [Upcoming audits](https://code4rena.com/audits#upcoming-audits)
- [Past audits](https://code4rena.com/audits#completed-audits)
- [Bounties](https://code4rena.com/bounties)
[Ongoing audits](https://code4rena.com/audits#active-audits)
[Upcoming audits](https://code4rena.com/audits#upcoming-audits)
[Past audits](https://code4rena.com/audits#completed-audits)
[Bounties](https://code4rena.com/bounties)
- [Past 90 days](https://code4rena.com/leaderboard?timeframe=Last 90 days)
- [Past 365 days](https://code4rena.com/leaderboard?timeframe=Last 365 days)
- [All time](https://code4rena.com/leaderboard?timeframe=All time)
[Past 90 days](https://code4rena.com/leaderboard?timeframe=Last 90 days)
[Past 365 days](https://code4rena.com/leaderboard?timeframe=Last 365 days)
[All time](https://code4rena.com/leaderboard?timeframe=All time)
- [Reports](https://code4rena.com/reports)
- [Help desk](https://code4rena.com/help)
- [Docs](https://docs.code4rena.com)
- [GitHub](https://github.com/code-423n4/)
[Reports](https://code4rena.com/reports)
[Help desk](https://code4rena.com/help)
[Docs](https://docs.code4rena.com)
[GitHub](https://github.com/code-423n4/)
[Support](https://code4rena.com/help)
[Log in](https://code4rena.com/login)

## Login
[Log in / Register](https://code4rena.com/login)
[Get an audit](https://go.code4rena.com/start?source=header-nav)

#### 2026-01-27

- 
[Overview](https://code4rena.com/reports/2025-11-merkl#overview)

[About C4](https://code4rena.com/reports/2025-11-merkl#about-c4)


- [About C4](https://code4rena.com/reports/2025-11-merkl#about-c4)
- [Summary](https://code4rena.com/reports/2025-11-merkl#summary)
- [Scope](https://code4rena.com/reports/2025-11-merkl#scope)
- [Severity Criteria](https://code4rena.com/reports/2025-11-merkl#severity-criteria)
- 
[Medium Risk Findings (4)](https://code4rena.com/reports/2025-11-merkl#medium-risk-findings-4)

[[M-01] Minimum Reward-Per-Hour Validation Applied to Gross Instead of Net Amount](https://code4rena.com/reports/2025-11-merkl#m-01-minimum-reward-per-hour-validation-applied-to-gross-instead-of-net-amount)
[[M-02] Improper Error Handling of onClaim Callback in _claim Function](https://code4rena.com/reports/2025-11-merkl#m-02-improper-error-handling-of-onclaim-callback-in-_claim-function)
[[M-03] Multi-step campaign overrides are anchored to the original campaign, making later overrides impossible](https://code4rena.com/reports/2025-11-merkl#m-03-multi-step-campaign-overrides-are-anchored-to-the-original-campaign-making-later-overrides-impossible)


- [[M-01] Minimum Reward-Per-Hour Validation Applied to Gross Instead of Net Amount](https://code4rena.com/reports/2025-11-merkl#m-01-minimum-reward-per-hour-validation-applied-to-gross-instead-of-net-amount)
- [[M-02] Improper Error Handling of onClaim Callback in _claim Function](https://code4rena.com/reports/2025-11-merkl#m-02-improper-error-handling-of-onclaim-callback-in-_claim-function)
- [[M-03] Multi-step campaign overrides are anchored to the original campaign, making later overrides impossible](https://code4rena.com/reports/2025-11-merkl#m-03-multi-step-campaign-overrides-are-anchored-to-the-original-campaign-making-later-overrides-impossible)
- 
[Low Risk and Informational Issues](https://code4rena.com/reports/2025-11-merkl#low-risk-and-informational-issues)

[L-01 resolveDispute reverts when disputer is blacklisted causing dispute resolution deadlock](https://code4rena.com/reports/2025-11-merkl#l-01-resolvedispute-reverts-when-disputer-is-blacklisted-causing-dispute-resolution-deadlock)
[L-02 overrideCampaign end timestamp validation uses wrong variable allowing past campaign timestamps](https://code4rena.com/reports/2025-11-merkl#l-02-overridecampaign-end-timestamp-validation-uses-wrong-variable-allowing-past-campaign-timestamps)
[L-03 overrideCampaign missing reward rate validation allows bypass of minimum amount restriction](https://code4rena.com/reports/2025-11-merkl#l-03-overridecampaign-missing-reward-rate-validation-allows-bypass-of-minimum-amount-restriction)
[L-04 _createCampaign validates minimum rate on gross amount before fee deduction](https://code4rena.com/reports/2025-11-merkl#l-04-_createcampaign-validates-minimum-rate-on-gross-amount-before-fee-deduction)
[L-05 getMerkleRoot returns old tree during dispute period allowing claims at stale rates](https://code4rena.com/reports/2025-11-merkl#l-05-getmerkleroot-returns-old-tree-during-dispute-period-allowing-claims-at-stale-rates)


- [L-01 resolveDispute reverts when disputer is blacklisted causing dispute resolution deadlock](https://code4rena.com/reports/2025-11-merkl#l-01-resolvedispute-reverts-when-disputer-is-blacklisted-causing-dispute-resolution-deadlock)
- [L-02 overrideCampaign end timestamp validation uses wrong variable allowing past campaign timestamps](https://code4rena.com/reports/2025-11-merkl#l-02-overridecampaign-end-timestamp-validation-uses-wrong-variable-allowing-past-campaign-timestamps)
- [L-03 overrideCampaign missing reward rate validation allows bypass of minimum amount restriction](https://code4rena.com/reports/2025-11-merkl#l-03-overridecampaign-missing-reward-rate-validation-allows-bypass-of-minimum-amount-restriction)
- [L-04 _createCampaign validates minimum rate on gross amount before fee deduction](https://code4rena.com/reports/2025-11-merkl#l-04-_createcampaign-validates-minimum-rate-on-gross-amount-before-fee-deduction)
- [L-05 getMerkleRoot returns old tree during dispute period allowing claims at stale rates](https://code4rena.com/reports/2025-11-merkl#l-05-getmerkleroot-returns-old-tree-during-dispute-period-allowing-claims-at-stale-rates)
- [Disclosures](https://code4rena.com/reports/2025-11-merkl#disclosures)
[Overview](https://code4rena.com/reports/2025-11-merkl#overview)
- [About C4](https://code4rena.com/reports/2025-11-merkl#about-c4)

[About C4](https://code4rena.com/reports/2025-11-merkl#about-c4)
[Summary](https://code4rena.com/reports/2025-11-merkl#summary)
[Scope](https://code4rena.com/reports/2025-11-merkl#scope)
[Severity Criteria](https://code4rena.com/reports/2025-11-merkl#severity-criteria)
[Medium Risk Findings (4)](https://code4rena.com/reports/2025-11-merkl#medium-risk-findings-4)
- [[M-01] Minimum Reward-Per-Hour Validation Applied to Gross Instead of Net Amount](https://code4rena.com/reports/2025-11-merkl#m-01-minimum-reward-per-hour-validation-applied-to-gross-instead-of-net-amount)
- [[M-02] Improper Error Handling of onClaim Callback in _claim Function](https://code4rena.com/reports/2025-11-merkl#m-02-improper-error-handling-of-onclaim-callback-in-_claim-function)
- [[M-03] Multi-step campaign overrides are anchored to the original campaign, making later overrides impossible](https://code4rena.com/reports/2025-11-merkl#m-03-multi-step-campaign-overrides-are-anchored-to-the-original-campaign-making-later-overrides-impossible)
[[M-01] Minimum Reward-Per-Hour Validation Applied to Gross Instead of Net Amount](https://code4rena.com/reports/2025-11-merkl#m-01-minimum-reward-per-hour-validation-applied-to-gross-instead-of-net-amount)
[[M-02] Improper Error Handling of onClaim Callback in _claim Function](https://code4rena.com/reports/2025-11-merkl#m-02-improper-error-handling-of-onclaim-callback-in-_claim-function)

```
onClaim
```


```
_claim Function
```

[[M-03] Multi-step campaign overrides are anchored to the original campaign, making later overrides impossible](https://code4rena.com/reports/2025-11-merkl#m-03-multi-step-campaign-overrides-are-anchored-to-the-original-campaign-making-later-overrides-impossible)
[Low Risk and Informational Issues](https://code4rena.com/reports/2025-11-merkl#low-risk-and-informational-issues)
- [L-01 resolveDispute reverts when disputer is blacklisted causing dispute resolution deadlock](https://code4rena.com/reports/2025-11-merkl#l-01-resolvedispute-reverts-when-disputer-is-blacklisted-causing-dispute-resolution-deadlock)
- [L-02 overrideCampaign end timestamp validation uses wrong variable allowing past campaign timestamps](https://code4rena.com/reports/2025-11-merkl#l-02-overridecampaign-end-timestamp-validation-uses-wrong-variable-allowing-past-campaign-timestamps)
- [L-03 overrideCampaign missing reward rate validation allows bypass of minimum amount restriction](https://code4rena.com/reports/2025-11-merkl#l-03-overridecampaign-missing-reward-rate-validation-allows-bypass-of-minimum-amount-restriction)
- [L-04 _createCampaign validates minimum rate on gross amount before fee deduction](https://code4rena.com/reports/2025-11-merkl#l-04-_createcampaign-validates-minimum-rate-on-gross-amount-before-fee-deduction)
- [L-05 getMerkleRoot returns old tree during dispute period allowing claims at stale rates](https://code4rena.com/reports/2025-11-merkl#l-05-getmerkleroot-returns-old-tree-during-dispute-period-allowing-claims-at-stale-rates)
[L-01 resolveDispute reverts when disputer is blacklisted causing dispute resolution deadlock](https://code4rena.com/reports/2025-11-merkl#l-01-resolvedispute-reverts-when-disputer-is-blacklisted-causing-dispute-resolution-deadlock)

```
resolveDispute
```

[L-02 overrideCampaign end timestamp validation uses wrong variable allowing past campaign timestamps](https://code4rena.com/reports/2025-11-merkl#l-02-overridecampaign-end-timestamp-validation-uses-wrong-variable-allowing-past-campaign-timestamps)

```
overrideCampaign
```

[L-03 overrideCampaign missing reward rate validation allows bypass of minimum amount restriction](https://code4rena.com/reports/2025-11-merkl#l-03-overridecampaign-missing-reward-rate-validation-allows-bypass-of-minimum-amount-restriction)

```
overrideCampaign
```

[L-04 _createCampaign validates minimum rate on gross amount before fee deduction](https://code4rena.com/reports/2025-11-merkl#l-04-_createcampaign-validates-minimum-rate-on-gross-amount-before-fee-deduction)

```
_createCampaign
```

[L-05 getMerkleRoot returns old tree during dispute period allowing claims at stale rates](https://code4rena.com/reports/2025-11-merkl#l-05-getmerkleroot-returns-old-tree-during-dispute-period-allowing-claims-at-stale-rates)

```
getMerkleRoot
```

[Disclosures](https://code4rena.com/reports/2025-11-merkl#disclosures)

Code4rena (C4) is a competitive audit platform where security researchers, referred to as Wardens, review, audit, and analyze codebases for security vulnerabilities in exchange for bounties provided by sponsoring projects.
During the audit outlined in this document, C4 conducted an analysis of the Merkl smart contract system. The audit took place from November 25 to December 01, 2025.
Final report assembled by Code4rena.

# https://code4rena.com/reports/2025-11-merkl#summarySummary
The C4 analysis yielded an aggregated total of 3 unique vulnerabilities. Of these vulnerabilities, 0 received a risk rating in the category of HIGH severity and 3 received a risk rating in the category of MEDIUM severity.
Additionally, C4 analysis included 58 QA reports compiling issues with a risk rating of LOW severity or informational.
All of the issues presented here are linked back to their original finding, which may include relevant context from the judge and Merkl team.

# https://code4rena.com/reports/2025-11-merkl#scopeScope
The code under review can be found within the [C4 Merkl repository](https://github.com/code-423n4/2025-11-merkl), and is composed of 2 smart contracts written in the Solidity programming language and includes 604 lines of Solidity code.
The code in C4’s Merkl repository was pulled from:
- Repository: [https://github.com/AngleProtocol/merkl-contracts](https://github.com/AngleProtocol/merkl-contracts)
- Commit hash: b7bd0e65a3f366e4041bc83494cbd981f8852b16
[https://github.com/AngleProtocol/merkl-contracts](https://github.com/AngleProtocol/merkl-contracts)

```
b7bd0e65a3f366e4041bc83494cbd981f8852b16
```

# https://code4rena.com/reports/2025-11-merkl#severity-criteriaSeverity Criteria
C4 assesses the severity of disclosed vulnerabilities based on three primary risk categories: high, medium, and low/informational.
High-level considerations for vulnerabilities span the following key areas when conducting assessments:
- Malicious Input Handling
- Escalation of privileges
- Arithmetic
- Gas use
For more information regarding the severity criteria referenced throughout the submission review process, please refer to the documentation provided on [the C4 website](https://code4rena.com), specifically our section on [Severity Categorization](https://docs.code4rena.com/awarding/judging-criteria/severity-categorization).

[[M-01] Minimum Reward-Per-Hour Validation Applied to Gross Instead of Net Amount](https://code4rena.com/audits/2025-11-merkl/submissions/F-1)
Found by [V12](https://v12.zellic.io); also found by [blockace](https://code4rena.com/audits/2025-11-merkl/submissions/S-684), [felconsec](https://code4rena.com/audits/2025-11-merkl/submissions/S-502), [Glitchunter](https://code4rena.com/audits/2025-11-merkl/submissions/S-952), [hezze](https://code4rena.com/audits/2025-11-merkl/submissions/S-839), [KINGWEST](https://code4rena.com/audits/2025-11-merkl/submissions/S-739), [lonelybones](https://code4rena.com/audits/2025-11-merkl/submissions/S-266), [LonelyWolfDemon](https://code4rena.com/audits/2025-11-merkl/submissions/S-894), [N0nce](https://code4rena.com/audits/2025-11-merkl/submissions/S-875), [nachin](https://code4rena.com/audits/2025-11-merkl/submissions/S-132), [odeili](https://code4rena.com/audits/2025-11-merkl/submissions/S-215), [Scout007](https://code4rena.com/audits/2025-11-merkl/submissions/S-694), [slavina](https://code4rena.com/audits/2025-11-merkl/submissions/S-627), [SpicyMeatball](https://code4rena.com/audits/2025-11-merkl/submissions/S-424), [th3_hybrid](https://code4rena.com/audits/2025-11-merkl/submissions/S-200), and [Valves](https://code4rena.com/audits/2025-11-merkl/submissions/S-149)
- Contract: [DistributionCreator.sol #L526](https://github.com/code-423n4/2025-11-merkl/blob/e8c1d6f5a91c1144571c3ed238358a2b032dd32e/contracts/DistributionCreator.sol#L526)
- Function: _createCampaign (DistributionCreator)
[DistributionCreator.sol #L526](https://github.com/code-423n4/2025-11-merkl/blob/e8c1d6f5a91c1144571c3ed238358a2b032dd32e/contracts/DistributionCreator.sol#L526)

```
DistributionCreator.sol
```


```
_createCampaign (DistributionCreator)
```

The contract’s minimum reward-per-hour check is applied to the campaign’s gross amount before deducting protocol fees. After the check passes, fees are subtracted, reducing the net tokens distributed per hour below the required minimum. Because there is no re-validation after fee deduction, campaigns can appear compliant on-chain while actually underpaying recipients.

### https://code4rena.com/reports/2025-11-merkl#root-causeRoot cause
The code performs the minimum reward-per-hour validation against newCampaign.amount (gross) prior to computing and deducting fees. It never re-checks the threshold against the fee-adjusted (net) distribution amount.

### https://code4rena.com/reports/2025-11-merkl#impactImpact
Campaign creators can deploy campaigns that meet the on-chain minimum rate check but pay out at a lower rate than advertised. This undermines participant incentives, violates expected protocol guarantees, and can deceive users into accepting lower rewards.

[[M-02] Improper Error Handling of onClaim Callback in _claim Function](https://code4rena.com/audits/2025-11-merkl/submissions/F-2)

```
onClaim
```


```
_claim Function
```

Found by [V12](https://v12.zellic.io); also found by [Ahmerdrarerh](https://code4rena.com/audits/2025-11-merkl/submissions/S-667), [ayushblock](https://code4rena.com/audits/2025-11-merkl/submissions/S-542), [Bala1796](https://code4rena.com/audits/2025-11-merkl/submissions/S-849), [dantehrani](https://code4rena.com/audits/2025-11-merkl/submissions/S-576), [fathomhewclaim](https://code4rena.com/audits/2025-11-merkl/submissions/S-87), [grey](https://code4rena.com/audits/2025-11-merkl/submissions/S-525), [Guilherme](https://code4rena.com/audits/2025-11-merkl/submissions/S-39), [mrdafidi](https://code4rena.com/audits/2025-11-merkl/submissions/S-69), [Obito](https://code4rena.com/audits/2025-11-merkl/submissions/S-235), [rox_k](https://code4rena.com/audits/2025-11-merkl/submissions/S-57), [SpicyMeatball](https://code4rena.com/audits/2025-11-merkl/submissions/S-432), and [Supheli](https://code4rena.com/audits/2025-11-merkl/submissions/S-259)
- Contract: [Distributor.sol #L425](https://github.com/code-423n4/2025-11-merkl/blob/e8c1d6f5a91c1144571c3ed238358a2b032dd32e/contracts/Distributor.sol#L425)
- Function: _claim (Distributor)
[Distributor.sol #L425](https://github.com/code-423n4/2025-11-merkl/blob/e8c1d6f5a91c1144571c3ed238358a2b032dd32e/contracts/Distributor.sol#L425)

```
Distributor.sol
```


```
_claim (Distributor)
```

The Distributor contract’s internal _claim function uses a try/catch to isolate failures in an external onClaim callback, but misplaces and asymmetrically handles reverts and invalid return values. As implemented, successful external calls that return an unexpected magic value trigger a revert inside the try block (uncaught), while genuine external call failures are silently swallowed in the empty catch. This leads to inconsistent and undesirable outcomes: a malformed or malicious recipient can either block the entire batch or bypass critical post-claim logic.

```
_claim
```


```
onClaim
```


```
try
```


```
catch
```

### https://code4rena.com/reports/2025-11-merkl#root-cause-1Root cause
The try/catch surrounding the IClaimRecipient.onClaim callback is scoped only to the external call. Reverts or invalid returns detected inside the try success handler are not caught by the empty catch, and genuine external call failures are suppressed, resulting in both swallowed errors and uncaught reverts.

```
try/catch
```


```
IClaimRecipient.onClaim
```


```
try
```


```
catch
```

### https://code4rena.com/reports/2025-11-merkl#impact-1Impact
1. A malicious or buggy onClaim implementation can return an incorrect value, triggering an uncaught revert that aborts the entire claim batch and undoes successful token transfers (DOS risk and user fund rollback).
2. Conversely, a reverting onClaim call is silently ignored, allowing recipients to bypass intended callback logic (e.g., audit hooks or state updates), undermining protocol invariants.

```
onClaim
```


```
revert
```


```
onClaim
```

[[M-03] Multi-step campaign overrides are anchored to the original campaign, making later overrides impossible](https://code4rena.com/audits/2025-11-merkl/submissions/F-144)
Submitted by [v2110](https://code4rena.com/audits/2025-11-merkl/submissions/S-865), also found by [ahahaHard1k](https://code4rena.com/audits/2025-11-merkl/submissions/S-452), [araj](https://code4rena.com/audits/2025-11-merkl/submissions/S-396), [arturtoros](https://code4rena.com/audits/2025-11-merkl/submissions/S-844), [drdee](https://code4rena.com/audits/2025-11-merkl/submissions/S-540), [kovacs7](https://code4rena.com/audits/2025-11-merkl/submissions/S-71), [peazzycole](https://code4rena.com/audits/2025-11-merkl/submissions/S-590), [phoenixV110](https://code4rena.com/audits/2025-11-merkl/submissions/S-332), and [slavina](https://code4rena.com/audits/2025-11-merkl/submissions/S-119)
Contract: [DistributionCreator.sol #L237](https://github.com/code-423n4/2025-11-merkl/blob/e8c1d6f5a91c1144571c3ed238358a2b032dd32e/contracts/DistributionCreator.sol#L237)

```
DistributionCreator.sol
```

overrideCampaign always validates a new override against the original campaign parameters stored in campaignList, not against any previously stored override. As a result, once a campaign has been overridden, any later override is still constrained by the original startTimestamp / duration, not by the latest override.

```
overrideCampaign
```


```
original
```


```
campaignList
```


```
startTimestamp
```


```
duration
```

The override mechanism effectively becomes single-use in many realistic scenarios:

```
single-use
```

1. Suppose a creator:
2. Creates a campaign with startTimestamp = 1000, duration = 3600.
3. Before the original start, calls overrideCampaign to move the start to 2000 and extend duration to 7200.
4. Later, at block.timestamp = 1500, the creator (or operator) wants to adjust parameters again (e.g., tweak duration or start).

```
startTimestamp = 1000
```


```
duration = 3600
```


```
overrideCampaign
```


```
2000
```


```
7200
```


```
block.timestamp = 1500
```

Because the validation is still comparing against the original: block.timestamp > _campaign.startTimestamp (1500 > 1000) is true.

```
block.timestamp > _campaign.startTimestamp
```

If newCampaign.startTimestamp != _campaign.startTimestamp (e.g., 2000 vs original 1000), the condition: (newCampaign.startTimestamp != _campaign.startTimestamp && block.timestamp > _campaign.startTimestamp) becomes true and the transaction reverts with InvalidOverride.

```
newCampaign.startTimestamp != _campaign.startTimestamp
```


```
(newCampaign.startTimestamp != _campaign.startTimestamp && block.timestamp > _campaign.startTimestamp)
```


```
true
```


```
InvalidOverride
```

Even though the effective campaign (in the off-chain engine) hasn’t started yet (new start at 2000), the contract refuses a new override due to the original start being in the past.
This leads to:
- Creators being unable to adjust campaigns in multiple steps.
- Operators seeing a revert even when, from a business perspective, the campaign should still be modifiable.
Given Merkl’s model where campaign creators should have flexible control over their campaigns, this is a meaningful functional break, not just cosmetic.

Change overrideCampaign to validate against the effective campaign parameters:

```
overrideCampaign
```


```
effective campaign parameters
```

- 
When there is an override:
CampaignParameters memory base = campaignOverrides[_campaignId].campaignId == _campaignId
? campaignOverrides[_campaignId]
: campaignList[campaignLookup(_campaignId)];

- Use base for all validation instead of the original campaignList entry.
- This aligns on-chain constraints with how off-chain logic is expected to interpret campaigns.
When there is an override:

```
CampaignParameters memory base = campaignOverrides[_campaignId].campaignId == _campaignId ? campaignOverrides[_campaignId] : campaignList[campaignLookup(_campaignId)];
```


```
CampaignParameters memory base = campaignOverrides[_campaignId].campaignId == _campaignId ? campaignOverrides[_campaignId] : campaignList[campaignLookup(_campaignId)];
```


```
campaignList
```

[View detailed Proof of Concept](https://code4rena.com/audits/2025-11-merkl/submissions/S-865)

For this audit, 58 QA reports were submitted by wardens compiling low risk and informational issues. The [QA report highlighted below](https://code4rena.com/audits/2025-11-merkl/submissions/S-623) by slvDev received the top score from the judge. 15 Low-severity findings were also submitted individually, and can be viewed [here](https://code4rena.com/audits/2025-11-merkl/submissions?groupByPrimary=true&severity=low).

The following wardens also submitted QA reports: [0xBug_X](https://code4rena.com/audits/2025-11-merkl/submissions/S-921), [0xD4n13l](https://code4rena.com/audits/2025-11-merkl/submissions/S-313), [0xFBI](https://code4rena.com/audits/2025-11-merkl/submissions/S-70), [0xnija](https://code4rena.com/audits/2025-11-merkl/submissions/S-962), [0xpetern](https://code4rena.com/audits/2025-11-merkl/submissions/S-762), [0xsai](https://code4rena.com/audits/2025-11-merkl/submissions/S-386), [0xscater](https://code4rena.com/audits/2025-11-merkl/submissions/S-240), [0xzerpa](https://code4rena.com/audits/2025-11-merkl/submissions/S-454), [AasifUsmani](https://code4rena.com/audits/2025-11-merkl/submissions/S-765), [Ahmerdrarerh](https://code4rena.com/audits/2025-11-merkl/submissions/S-508), [ameng](https://code4rena.com/audits/2025-11-merkl/submissions/S-271), [Aristos](https://code4rena.com/audits/2025-11-merkl/submissions/S-987), [arunabha003](https://code4rena.com/audits/2025-11-merkl/submissions/S-654), [aua_oo7](https://code4rena.com/audits/2025-11-merkl/submissions/S-909), [Ayomiposi233](https://code4rena.com/audits/2025-11-merkl/submissions/S-960), [Bobai23](https://code4rena.com/audits/2025-11-merkl/submissions/S-514), [Bube](https://code4rena.com/audits/2025-11-merkl/submissions/S-747), [desaperh](https://code4rena.com/audits/2025-11-merkl/submissions/S-825), [Diavolo](https://code4rena.com/audits/2025-11-merkl/submissions/S-176), [farismaulana](https://code4rena.com/audits/2025-11-merkl/submissions/S-397), [felconsec](https://code4rena.com/audits/2025-11-merkl/submissions/S-509), [francoHacker](https://code4rena.com/audits/2025-11-merkl/submissions/S-974), [grey](https://code4rena.com/audits/2025-11-merkl/submissions/S-527), [hezze](https://code4rena.com/audits/2025-11-merkl/submissions/S-881), [holtzzx](https://code4rena.com/audits/2025-11-merkl/submissions/S-874), [home1344](https://code4rena.com/audits/2025-11-merkl/submissions/S-81), [iam_emptyset](https://code4rena.com/audits/2025-11-merkl/submissions/S-239), [inh3l](https://code4rena.com/audits/2025-11-merkl/submissions/S-689), [itsravin0x](https://code4rena.com/audits/2025-11-merkl/submissions/S-914), [jerry0422](https://code4rena.com/audits/2025-11-merkl/submissions/S-994), [kestyvickky](https://code4rena.com/audits/2025-11-merkl/submissions/S-177), [khaye26](https://code4rena.com/audits/2025-11-merkl/submissions/S-910), [kovacs7](https://code4rena.com/audits/2025-11-merkl/submissions/S-591), [kwad](https://code4rena.com/audits/2025-11-merkl/submissions/S-264), [mahdifa](https://code4rena.com/audits/2025-11-merkl/submissions/S-635), [Manvita](https://code4rena.com/audits/2025-11-merkl/submissions/S-471), [Meks079](https://code4rena.com/audits/2025-11-merkl/submissions/S-238), [Oxhsn](https://code4rena.com/audits/2025-11-merkl/submissions/S-981), [oziajibogu](https://code4rena.com/audits/2025-11-merkl/submissions/S-644), [pfapostol](https://code4rena.com/audits/2025-11-merkl/submissions/S-728), [phoenixV110](https://code4rena.com/audits/2025-11-merkl/submissions/S-582), [PriorToHuman](https://code4rena.com/audits/2025-11-merkl/submissions/S-744), [rare_one](https://code4rena.com/audits/2025-11-merkl/submissions/S-338), [redfox](https://code4rena.com/audits/2025-11-merkl/submissions/S-306), [renacoder](https://code4rena.com/audits/2025-11-merkl/submissions/S-958), [Rikka](https://code4rena.com/audits/2025-11-merkl/submissions/S-616), [s4bot3ur](https://code4rena.com/audits/2025-11-merkl/submissions/S-730), [Shawon](https://code4rena.com/audits/2025-11-merkl/submissions/S-809), [Sparrow](https://code4rena.com/audits/2025-11-merkl/submissions/S-993), [sudais_b](https://code4rena.com/audits/2025-11-merkl/submissions/S-257), [The_Amazing_One](https://code4rena.com/audits/2025-11-merkl/submissions/S-584), [TheCarrot](https://code4rena.com/audits/2025-11-merkl/submissions/S-641), [unnamed](https://code4rena.com/audits/2025-11-merkl/submissions/S-890), [Volleyking](https://code4rena.com/audits/2025-11-merkl/submissions/S-832), [Yuubee](https://code4rena.com/audits/2025-11-merkl/submissions/S-65), [ZeronautX](https://code4rena.com/audits/2025-11-merkl/submissions/S-784), and [zulkifilu01](https://code4rena.com/audits/2025-11-merkl/submissions/S-614).

```
resolveDispute
```

When disputer address becomes blacklisted (e.g. USDC blacklist), the resolveDispute(true) function reverts because it cannot transfer tokens back to disputer. This creates deadlock where Governor cannot validate legitimate dispute and also cannot call revokeTree() due to unresolved dispute check.

```
resolveDispute(true)
```


```
revokeTree()
```

### https://code4rena.com/reports/2025-11-merkl#finding-description-and-impactFinding description and impact
The Distributor contract uses push pattern for returning dispute deposits. When Governor resolves dispute as valid, the contract tries to transfer disputeAmount back to disputer:

```
Distributor
```


```
disputeAmount
```


```
function resolveDispute(bool valid) external onlyGovernor { if (disputer == address(0)) revert Errors.NoDispute(); if (valid) { IERC20(disputeToken).safeTransfer(disputer, disputeAmount); // reverts if blacklisted _revokeTree(); } else { IERC20(disputeToken).safeTransfer(msg.sender, disputeAmount); endOfDisputePeriod = _endOfDisputePeriod(uint48(block.timestamp)); } disputer = address(0); emit DisputeResolved(valid); }
```


```
function resolveDispute(bool valid) external onlyGovernor { if (disputer == address(0)) revert Errors.NoDispute(); if (valid) { IERC20(disputeToken).safeTransfer(disputer, disputeAmount); // reverts if blacklisted _revokeTree(); } else { IERC20(disputeToken).safeTransfer(msg.sender, disputeAmount); endOfDisputePeriod = _endOfDisputePeriod(uint48(block.timestamp)); } disputer = address(0); emit DisputeResolved(valid); }
```

If disputer is blacklisted by token issuer (like Circle for USDC), the safeTransfer will revert. Governor then tries to call revokeTree() directly but this also fails:

```
safeTransfer
```


```
revokeTree()
```


```
function revokeTree() external onlyGovernor { if (disputer != address(0)) revert Errors.UnresolvedDispute(); // blocked! _revokeTree(); }
```


```
function revokeTree() external onlyGovernor { if (disputer != address(0)) revert Errors.UnresolvedDispute(); // blocked! _revokeTree(); }
```

This is problematic because:
1. USDC is very common token and likely to be used as dispute token
2. Any address can get blacklisted for compliance reasons (not always user fault)
3. Attacker who submits malicious tree could try to get legitimate disputer blacklisted
4. Governor is stuck - cannot validate dispute, cannot revoke tree directly
The only option left for Governor is resolveDispute(false) which:

```
resolveDispute(false)
```

- Sends deposit to Governor instead of disputer (unfair to honest disputer)
- Does NOT revoke the potentially malicious tree
- Extends dispute period instead of fixing the issue

### https://code4rena.com/reports/2025-11-merkl#recommended-mitigation-steps-1Recommended mitigation steps
Use pull pattern instead of push for dispute refunds:

```
mapping(address => uint256) public pendingRefunds; function resolveDispute(bool valid) external onlyGovernor { if (disputer == address(0)) revert Errors.NoDispute(); if (valid) { pendingRefunds[disputer] += disputeAmount; // store for withdrawal _revokeTree(); } else { IERC20(disputeToken).safeTransfer(msg.sender, disputeAmount); endOfDisputePeriod = _endOfDisputePeriod(uint48(block.timestamp)); } disputer = address(0); emit DisputeResolved(valid); } function claimRefund() external { uint256 amount = pendingRefunds[msg.sender]; pendingRefunds[msg.sender] = 0; IERC20(disputeToken).safeTransfer(msg.sender, amount); }
```


```
mapping(address => uint256) public pendingRefunds; function resolveDispute(bool valid) external onlyGovernor { if (disputer == address(0)) revert Errors.NoDispute(); if (valid) { pendingRefunds[disputer] += disputeAmount; // store for withdrawal _revokeTree(); } else { IERC20(disputeToken).safeTransfer(msg.sender, disputeAmount); endOfDisputePeriod = _endOfDisputePeriod(uint48(block.timestamp)); } disputer = address(0); emit DisputeResolved(valid); } function claimRefund() external { uint256 amount = pendingRefunds[msg.sender]; pendingRefunds[msg.sender] = 0; IERC20(disputeToken).safeTransfer(msg.sender, amount); }
```

This way resolveDispute(true) always succeeds and disputer can claim refund when they are able to recieve tokens.

```
resolveDispute(true)
```

```
overrideCampaign
```

The overrideCampaign function in DistributionCreator contract has a validation bug on line 244. The code comment says “End timestamp should be in the future” but the check uses wrong variable - it uses _campaign.startTimestamp (old/original value) instead of newCampaign.startTimestamp (new value being set):

```
overrideCampaign
```


```
DistributionCreator
```


```
_campaign.startTimestamp
```


```
newCampaign.startTimestamp
```


```
function overrideCampaign(bytes32 _campaignId, CampaignParameters memory newCampaign) external { CampaignParameters memory _campaign = campaign(_campaignId); _isValidOperator(_campaign.creator); if ( newCampaign.rewardToken != _campaign.rewardToken || newCampaign.amount != _campaign.amount || (newCampaign.startTimestamp != _campaign.startTimestamp && block.timestamp > _campaign.startTimestamp) || // End timestamp should be in the future newCampaign.duration + _campaign.startTimestamp <= block.timestamp // BUG: uses OLD start ) revert Errors.InvalidOverride(); // ... }
```


```
function overrideCampaign(bytes32 _campaignId, CampaignParameters memory newCampaign) external { CampaignParameters memory _campaign = campaign(_campaignId); _isValidOperator(_campaign.creator); if ( newCampaign.rewardToken != _campaign.rewardToken || newCampaign.amount != _campaign.amount || (newCampaign.startTimestamp != _campaign.startTimestamp && block.timestamp > _campaign.startTimestamp) || // End timestamp should be in the future newCampaign.duration + _campaign.startTimestamp <= block.timestamp // BUG: uses OLD start ) revert Errors.InvalidOverride(); // ... }
```

The check should be:

```
newCampaign.duration + newCampaign.startTimestamp <= block.timestamp // CORRECT: uses NEW start
```


```
newCampaign.duration + newCampaign.startTimestamp <= block.timestamp // CORRECT: uses NEW start
```

Because of this bug, campaign creator can set both startTimestamp and end time to the past. Attack works like this:

```
startTimestamp
```

1. Creator makes campaign with future start (e.g. block.timestamp + 10 days)
2. Creator calls override with past start (e.g. block.timestamp - 10000) and short duration
3. Validation passes because it checks newDuration + OLD_startTimestamp which is still in future
4. Campaign now has start and end timestamps both in the past

```
block.timestamp + 10 days
```


```
block.timestamp - 10000
```


```
newDuration + OLD_startTimestamp
```

The impact is limited because:
- reallocateCampaignRewards function uses original timestamps from campaign() getter, not from override
- NatSpec says “invalid overrides are ignored” by off-chain Merkl engine
- Creator cannot directly extract funds back

```
reallocateCampaignRewards
```


```
campaign()
```

However this violates the intended invariant that “End timestamp should be in the future” and could confuse off-chain processing if engine doesnt validate timestamps properly.

Fix the validation to use the new startTimestamp value:

```
startTimestamp
```


```
function overrideCampaign(bytes32 _campaignId, CampaignParameters memory newCampaign) external { CampaignParameters memory _campaign = campaign(_campaignId); _isValidOperator(_campaign.creator); if ( newCampaign.rewardToken != _campaign.rewardToken || newCampaign.amount != _campaign.amount || (newCampaign.startTimestamp != _campaign.startTimestamp && block.timestamp > _campaign.startTimestamp) || // End timestamp should be in the future - use NEW startTimestamp newCampaign.duration + newCampaign.startTimestamp <= block.timestamp ) revert Errors.InvalidOverride(); newCampaign.campaignId = _campaignId; newCampaign.creator = _campaign.creator; campaignOverrides[_campaignId] = newCampaign; campaignOverridesTimestamp[_campaignId].push(block.timestamp); emit CampaignOverride(_campaignId, newCampaign); }
```


```
function overrideCampaign(bytes32 _campaignId, CampaignParameters memory newCampaign) external { CampaignParameters memory _campaign = campaign(_campaignId); _isValidOperator(_campaign.creator); if ( newCampaign.rewardToken != _campaign.rewardToken || newCampaign.amount != _campaign.amount || (newCampaign.startTimestamp != _campaign.startTimestamp && block.timestamp > _campaign.startTimestamp) || // End timestamp should be in the future - use NEW startTimestamp newCampaign.duration + newCampaign.startTimestamp <= block.timestamp ) revert Errors.InvalidOverride(); newCampaign.campaignId = _campaignId; newCampaign.creator = _campaign.creator; campaignOverrides[_campaignId] = newCampaign; campaignOverridesTimestamp[_campaignId].push(block.timestamp); emit CampaignOverride(_campaignId, newCampaign); }
```

```
overrideCampaign
```

The DistributionCreator contract has a mechanism to enforce minimum reward rates per epoch through rewardTokenMinAmounts mapping. This check is properly implemented in _createCampaign function:

```
DistributionCreator
```


```
rewardTokenMinAmounts
```


```
_createCampaign
```


```
function _createCampaign(CampaignParameters memory newCampaign) internal returns (bytes32) { uint256 rewardTokenMinAmount = rewardTokenMinAmounts[newCampaign.rewardToken]; // if the campaign doesn't last at least one hour if (newCampaign.duration < HOUR) revert Errors.CampaignDurationBelowHour(); // if the reward token is not whitelisted as an incentive token if (rewardTokenMinAmount == 0) revert Errors.CampaignRewardTokenNotWhitelisted(); // if the amount distributed is too small with respect to what is allowed if ((newCampaign.amount * HOUR) / newCampaign.duration < rewardTokenMinAmount) revert Errors.CampaignRewardTooLow(); // ... }
```


```
function _createCampaign(CampaignParameters memory newCampaign) internal returns (bytes32) { uint256 rewardTokenMinAmount = rewardTokenMinAmounts[newCampaign.rewardToken]; // if the campaign doesn't last at least one hour if (newCampaign.duration < HOUR) revert Errors.CampaignDurationBelowHour(); // if the reward token is not whitelisted as an incentive token if (rewardTokenMinAmount == 0) revert Errors.CampaignRewardTokenNotWhitelisted(); // if the amount distributed is too small with respect to what is allowed if ((newCampaign.amount * HOUR) / newCampaign.duration < rewardTokenMinAmount) revert Errors.CampaignRewardTooLow(); // ... }
```

However, the overrideCampaign function allows campaign creators to modify the duration parameter without re-validating this minimum rate check:

```
overrideCampaign
```


```
duration
```


```
function overrideCampaign(bytes32 _campaignId, CampaignParameters memory newCampaign) external { CampaignParameters memory _campaign = campaign(_campaignId); _isValidOperator(_campaign.creator); if ( newCampaign.rewardToken != _campaign.rewardToken || newCampaign.amount != _campaign.amount || (newCampaign.startTimestamp != _campaign.startTimestamp && block.timestamp > _campaign.startTimestamp) || newCampaign.duration + _campaign.startTimestamp <= block.timestamp ) revert Errors.InvalidOverride(); newCampaign.campaignId = _campaignId; newCampaign.creator = _campaign.creator; campaignOverrides[_campaignId] = newCampaign; // ... }
```


```
function overrideCampaign(bytes32 _campaignId, CampaignParameters memory newCampaign) external { CampaignParameters memory _campaign = campaign(_campaignId); _isValidOperator(_campaign.creator); if ( newCampaign.rewardToken != _campaign.rewardToken || newCampaign.amount != _campaign.amount || (newCampaign.startTimestamp != _campaign.startTimestamp && block.timestamp > _campaign.startTimestamp) || newCampaign.duration + _campaign.startTimestamp <= block.timestamp ) revert Errors.InvalidOverride(); newCampaign.campaignId = _campaignId; newCampaign.creator = _campaign.creator; campaignOverrides[_campaignId] = newCampaign; // ... }
```

The function validates that rewardToken and amount cannot change, but it does not check if the new duration value would result in reward rate below the minimum threshold.

```
rewardToken
```


```
amount
```


```
duration
```

A user can exploit this by:
1. Creating campaign with compliant rate (e.g., 1.8e8 tokens for 1 hour = 1.8e8/epoch rate)
2. Calling overrideCampaign to extend duration to 10 hours
3. New rate becomes 1.8e7/epoch which is below minimum of 1e8/epoch

```
overrideCampaign
```

This bypass allows creation of “dust” campaigns that distribute very small amounts per epoch, which the rewardTokenMinAmounts restriction was designed to prevent. The governance-set spam protection is rendered ineffective through this two-step process.

```
rewardTokenMinAmounts
```

Add the minimum reward rate validation to overrideCampaign function:

```
overrideCampaign
```


```
function overrideCampaign(bytes32 _campaignId, CampaignParameters memory newCampaign) external { CampaignParameters memory _campaign = campaign(_campaignId); _isValidOperator(_campaign.creator); if ( newCampaign.rewardToken != _campaign.rewardToken || newCampaign.amount != _campaign.amount || (newCampaign.startTimestamp != _campaign.startTimestamp && block.timestamp > _campaign.startTimestamp) || newCampaign.duration + _campaign.startTimestamp <= block.timestamp ) revert Errors.InvalidOverride(); // Add minimum rate validation uint256 rewardTokenMinAmount = rewardTokenMinAmounts[newCampaign.rewardToken]; if ((newCampaign.amount * HOUR) / newCampaign.duration < rewardTokenMinAmount) revert Errors.CampaignRewardTooLow(); newCampaign.campaignId = _campaignId; newCampaign.creator = _campaign.creator; campaignOverrides[_campaignId] = newCampaign; campaignOverridesTimestamp[_campaignId].push(block.timestamp); emit CampaignOverride(_campaignId, newCampaign); }
```


```
function overrideCampaign(bytes32 _campaignId, CampaignParameters memory newCampaign) external { CampaignParameters memory _campaign = campaign(_campaignId); _isValidOperator(_campaign.creator); if ( newCampaign.rewardToken != _campaign.rewardToken || newCampaign.amount != _campaign.amount || (newCampaign.startTimestamp != _campaign.startTimestamp && block.timestamp > _campaign.startTimestamp) || newCampaign.duration + _campaign.startTimestamp <= block.timestamp ) revert Errors.InvalidOverride(); // Add minimum rate validation uint256 rewardTokenMinAmount = rewardTokenMinAmounts[newCampaign.rewardToken]; if ((newCampaign.amount * HOUR) / newCampaign.duration < rewardTokenMinAmount) revert Errors.CampaignRewardTooLow(); newCampaign.campaignId = _campaignId; newCampaign.creator = _campaign.creator; campaignOverrides[_campaignId] = newCampaign; campaignOverridesTimestamp[_campaignId].push(block.timestamp); emit CampaignOverride(_campaignId, newCampaign); }
```

```
_createCampaign
```

The _createCampaign function in DistributionCreator.sol validates that campaign reward rate meets the minimum requirement set by governance. However, this validation is performed on the gross amount (before fees) while the actual stored amount is the net amount (after fees are deducted).

```
_createCampaign
```


```
DistributionCreator.sol
```


```
function _createCampaign(CampaignParameters memory newCampaign) internal returns (bytes32) { uint256 rewardTokenMinAmount = rewardTokenMinAmounts[newCampaign.rewardToken]; // ... // if the amount distributed is too small with respect to what is allowed if ((newCampaign.amount * HOUR) / newCampaign.duration < rewardTokenMinAmount) revert Errors.CampaignRewardTooLow(); // Computing fees and pulling tokens uint256 campaignAmountMinusFees = _computeFees(newCampaign.campaignType, newCampaign.amount); // ... newCampaign.amount = campaignAmountMinusFees; // NET amount stored // ... }
```


```
function _createCampaign(CampaignParameters memory newCampaign) internal returns (bytes32) { uint256 rewardTokenMinAmount = rewardTokenMinAmounts[newCampaign.rewardToken]; // ... // if the amount distributed is too small with respect to what is allowed if ((newCampaign.amount * HOUR) / newCampaign.duration < rewardTokenMinAmount) revert Errors.CampaignRewardTooLow(); // Computing fees and pulling tokens uint256 campaignAmountMinusFees = _computeFees(newCampaign.campaignType, newCampaign.amount); // ... newCampaign.amount = campaignAmountMinusFees; // NET amount stored // ... }
```

The problem is that minimum rate check at line 533 uses newCampaign.amount which is gross amount before fees. But after _computeFees is called, the newCampaign.amount gets overwritten with net amount at line 538. This means campaigns can be created with actual reward rate below the governance-set minimum.

```
newCampaign.amount
```


```
_computeFees
```


```
newCampaign.amount
```

Example with 10% protocol fees:
1. Governance sets rewardTokenMinAmount = 1e8 for a token
2. User creates 1-hour campaign with exactly amount = 1e8
3. Validation passes: (1e8 * 3600) / 3600 = 1e8 >= 1e8
4. After 10% fees deducted: campaignAmountMinusFees = 0.9e8
5. Campaign is stored with rate of 0.9e8 per epoch, which is 10% below minimum

```
rewardTokenMinAmount = 1e8
```


```
amount = 1e8
```


```
(1e8 * 3600) / 3600 = 1e8 >= 1e8
```


```
campaignAmountMinusFees = 0.9e8
```


```
0.9e8
```

The rewardTokenMinAmounts mapping exists to prevent dust campaigns that spam the system. This validation order allows campaigns to bypass this protection by the exact fee percentage amount.

```
rewardTokenMinAmounts
```

Move the minimum rate validation after fee calculation so it checks the net amount:

```
function _createCampaign(CampaignParameters memory newCampaign) internal returns (bytes32) { uint256 rewardTokenMinAmount = rewardTokenMinAmounts[newCampaign.rewardToken]; if (newCampaign.duration < HOUR) revert Errors.CampaignDurationBelowHour(); if (rewardTokenMinAmount == 0) revert Errors.CampaignRewardTokenNotWhitelisted(); // Computing fees first uint256 campaignAmountMinusFees = _computeFees(newCampaign.campaignType, newCampaign.amount); // Then validate minimum on NET amount if ((campaignAmountMinusFees * HOUR) / newCampaign.duration < rewardTokenMinAmount) revert Errors.CampaignRewardTooLow(); if (newCampaign.creator == address(0)) newCampaign.creator = msg.sender; _pullTokens(newCampaign.creator, newCampaign.rewardToken, newCampaign.amount, campaignAmountMinusFees); newCampaign.amount = campaignAmountMinusFees; // ... }
```


```
function _createCampaign(CampaignParameters memory newCampaign) internal returns (bytes32) { uint256 rewardTokenMinAmount = rewardTokenMinAmounts[newCampaign.rewardToken]; if (newCampaign.duration < HOUR) revert Errors.CampaignDurationBelowHour(); if (rewardTokenMinAmount == 0) revert Errors.CampaignRewardTokenNotWhitelisted(); // Computing fees first uint256 campaignAmountMinusFees = _computeFees(newCampaign.campaignType, newCampaign.amount); // Then validate minimum on NET amount if ((campaignAmountMinusFees * HOUR) / newCampaign.duration < rewardTokenMinAmount) revert Errors.CampaignRewardTooLow(); if (newCampaign.creator == address(0)) newCampaign.creator = msg.sender; _pullTokens(newCampaign.creator, newCampaign.rewardToken, newCampaign.amount, campaignAmountMinusFees); newCampaign.amount = campaignAmountMinusFees; // ... }
```

```
getMerkleRoot
```

When admin submits corrected merkle tree to fix erroneous rewards, the getMerkleRoot() function still returns old tree root during dispute period. Users can exploit this window to claim rewards at old (incorrect) rates before correction takes effect.

```
getMerkleRoot()
```

### https://code4rena.com/reports/2025-11-merkl#finding-description-and-impact-1Finding description and impact
The Distributor contract uses dispute period mechanism to protect against malicious tree updates. When new tree is submitted via updateTree(), the old tree becomes lastTree and new tree is stored in tree. During dispute period, getMerkleRoot() returns lastTree.merkleRoot instead of new one:

```
Distributor
```


```
updateTree()
```


```
lastTree
```


```
tree
```


```
getMerkleRoot()
```


```
lastTree.merkleRoot
```


```
function getMerkleRoot() public view returns (bytes32) { if (block.timestamp >= endOfDisputePeriod && disputer == address(0)) return tree.merkleRoot; else return lastTree.merkleRoot; // returns OLD tree during dispute }
```


```
function getMerkleRoot() public view returns (bytes32) { if (block.timestamp >= endOfDisputePeriod && disputer == address(0)) return tree.merkleRoot; else return lastTree.merkleRoot; // returns OLD tree during dispute }
```

This design assume old tree is always good and new tree might be bad. But when scenario is reversed - old tree has errors (like inflated rewards from off-chain computation bug) and new tree is the fix - users can front-run correction by claiming at old inflated rates.
The flow is:
1. Erroneous tree published with inflated rewards (e.g. alice gets 100 tokens)
2. Admin discovers error, submits corrected tree (alice should get 10 tokens)
3. During dispute period getMerkleRoot() still returns old root
4. Alice claims 100 tokens using old tree proofs
5. When dispute ends, alice already took 90 extra tokens

```
getMerkleRoot()
```

Impact is limited because:
- Requires off-chain computation error first (not contract bug)
- Governor can mitigate by doing double-update or draining funds via recoverERC20
- This is design limitation, not vulnerability

```
recoverERC20
```

However if admin does not react fast enough, users can drain more funds than they entitled to.

### https://code4rena.com/reports/2025-11-merkl#recommended-mitigation-steps-5Recommended mitigation steps
Governor should be aware that setDisputePeriod() does not affect current pending tree and corrections cannot be applied instantly. When erroneous tree is discovered, governor should either:

```
setDisputePeriod()
```

1. Use double-update pattern - submit correction twice so corrected tree becomes lastTree
2. Drain funds via recoverERC20() to prevent claims during investigation
3. Consider adding emergency freeze mechanism for claims (but this adds centralization risk)

```
lastTree
```


```
recoverERC20()
```

Alternative is to add function that allows governor to skip dispute period for specific tree update when fixing errors, but this reduces security guarantees of dispute mechanism.
Detailed Proofs of Concept for the above-listed Low-severity issues may be viewed [here](https://code4rena.com/audits/2025-11-merkl/submissions/S-623).

C4 audits incentivize the discovery of exploits, vulnerabilities, and bugs in smart contracts. Security researchers are rewarded at an increasing rate for finding higher-risk issues. Audit submissions are judged by a knowledgeable security researcher and disclosed to sponsoring developers. C4 does not conduct formal verification regarding the provided code but instead provides final verification.
C4 does not provide any guarantee or warranty regarding the security of this project. All smart contract software should be used at the sole risk and responsibility of users.
- [Twitter](https://twitter.com/code4rena)
- [Discord](https://discord.gg/code4rena)
- [GitHub](https://github.com/code-423n4/)
- [Media kit](https://github.com/code-423n4/media-kit)
- [Terms](https://docs.code4rena.com/legal/terms-of-service)
- [Privacy](https://docs.code4rena.com/legal/privacy-policy)
[Twitter](https://twitter.com/code4rena)
[Discord](https://discord.gg/code4rena)
[GitHub](https://github.com/code-423n4/)
[Media kit](https://github.com/code-423n4/media-kit)
[Terms](https://docs.code4rena.com/legal/terms-of-service)
[Privacy](https://docs.code4rena.com/legal/privacy-policy)

